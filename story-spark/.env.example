# Server configuration
PORT=3001

# Anthropic API key for story generation
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Note: Image generation uses Puter.js which requires NO API key
# It runs entirely client-side with a user-pays model
