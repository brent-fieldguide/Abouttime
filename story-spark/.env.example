# Server configuration (optional - server not required for Puter.js version)
PORT=3001

# NO API KEY NEEDED!
# This app uses Puter.js for both AI text generation and image generation.
# Puter.js is completely free and runs in the browser.
# Users may see a one-time Puter sign-in prompt on first use.
